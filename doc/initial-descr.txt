Программа заключается в следующем:
на вход подается n - число вершин в графе, случайным образом генерируется частотный вектор (вероятностный вектор - например, Р = (0,5; 0,2; 0,1; 0,1; 0,1) - сумма вероятностей должна быть равной 1). C этим вектором надо сделать 2 операции.
Первая - провести алгоритм Хаффмана и выдать локально-префиксный код,
Вторая - слить последние 2 вероятности в частотном векторе, упорядочить по невозрастанию, провести алгоритм Хаффмана и выдать локально-префиксный код (когда проведем второй алгоритм Хаффмана, необходимо тем вероятностям, которые мы слили на первом шаге приписать то кодовое слово, которое получилось у той слитой вероятности, т.е.одно и то же кодовое слово приписать 2 вершинам).
например, сольем последние 2 вероятности нашего примера
Р1 = (0,5; 0,2; 0,2; 0,1) - проведем алгоритм хаффмана, и получим
b1 - >0;
b2 ->11; // это тот код, которому соответствует слитая вероятность
b3 -> 100;
b4 -> 101;
V = (0,100, 101, 11, 11) - т.е.его мы формируем на основе Р. Вот, это и есть локально-префиксный код.
Надо получить и выдать их сразу оба.
